import { ApiErrorInterceptor } from './api-error-interceptor';

describe('ApiErrorInterceptor', () => {
  it('should create an instance', () => {
    expect(new ApiErrorInterceptor()).toBeTruthy();
  });
});
